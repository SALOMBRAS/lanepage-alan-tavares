import { Camera, Mail, Phone } from "lucide-react";
import { Container } from "@/components/ui";
import { cn } from "@/lib/utils";
import type { FooterProps } from "@/types";

/** Footer with the office's supplied contact channels. */
export function Footer({ data, className }: FooterProps) { const phoneHref = `tel:${data.phone.replace(/\D/g, "")}`; return <footer id="rodape" aria-label="Informações do escritório" className={cn("bg-primary py-12 text-primary-foreground", className)}><Container><div className="grid gap-10 md:grid-cols-[1.3fr_1fr_1fr]"><div><p className="font-heading text-3xl font-semibold">{data.brandName}</p><p className="mt-3 max-w-md leading-7 text-primary-foreground/75">Direito do Consumidor e Direito Criminal, com comunicação clara desde o primeiro contato.</p></div><div><h2 className="text-sm font-semibold uppercase tracking-[0.15em] text-secondary">Contato</h2><a href={phoneHref} className="mt-4 flex items-center gap-3 text-sm font-semibold hover:text-secondary focus-visible:ring-2 focus-visible:ring-secondary"><Phone aria-hidden="true" className="size-4" />{data.phone}</a><a href={data.socialLinks[0]?.href} target="_blank" rel="noreferrer" className="mt-3 flex items-center gap-3 text-sm font-semibold hover:text-secondary focus-visible:ring-2 focus-visible:ring-secondary"><Camera aria-hidden="true" className="size-4" />Instagram</a></div><div><h2 className="text-sm font-semibold uppercase tracking-[0.15em] text-secondary">Informações</h2><a href={`mailto:${data.email}`} className="mt-4 flex items-start gap-3 text-sm leading-6 text-primary-foreground/75 hover:text-secondary focus-visible:ring-2 focus-visible:ring-secondary"><Mail aria-hidden="true" className="mt-1 size-4 shrink-0" />{data.email}</a></div></div><div className="mt-10 flex flex-col gap-3 border-t border-primary-foreground/15 pt-6 text-xs text-primary-foreground/65 sm:flex-row sm:items-center sm:justify-between"><p>{data.copyright.replace("[ANO]", new Date().getFullYear().toString())}</p><p>{data.mockNotice}</p></div></Container></footer>; }
